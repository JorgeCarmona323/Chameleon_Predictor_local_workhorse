# Chameleonic Membrane Permeability is Size-Gated: Evidence from 3D Conformational Descriptors and Source-Stratified PAMPA Analysis

**Jorge Carmona | March 2026**

---

## Abstract

We present a computational pipeline for predicting cyclic peptide membrane permeability using 3D ensemble-derived conformational descriptors, with a focus on ΔPSA — the difference in polar surface area between aqueous and membrane-mimetic conformers. On a 1,566-compound source-stratified subset of CycPeptMPDB (Furukawa 2016 + Chugai), ensemble ΔPSA achieves AUC = 0.744 vs. AUC = 0.631 for MolLogP. UMAP clustering with molecular weight coloring reveals that the permeable cluster has a median MW of 1,180 Da vs. 820 Da for the impermeable cluster — a 1.44× gap that disappears entirely on the full heterogeneous 7,298-compound dataset. This finding, in convergence with Yu et al. 2026, supports the hypothesis that chameleonic membrane permeability is gated by molecular size at approximately 9 residues (~900 Da). Absolute ΔPSA is a partially confounded descriptor that scales with molecular size; per-residue normalization is the proposed next step.

---

## 1. Background

Passive membrane permeation for large cyclic peptides is not well-modeled by conventional 2D descriptors because these descriptors are conformationally blind. Cyclosporin A (CsA, MW 1,203 Da, 11 residues) is the archetypal example: it crosses membranes despite high polarity by adopting a compact conformation in apolar environments that buries backbone amide NH groups through intramolecular H-bonding. This *chameleonic* switching is invisible to descriptors like TPSA or MolLogP but should manifest as a large difference in 3D polar surface area (ΔPSA) between conformers sampled in aqueous vs. membrane-mimetic conditions.

CycPeptMPDB v1.2 provides the largest public collection of experimental cyclic peptide PAMPA data (8,466 compounds) but aggregates measurements from laboratories using incompatible protocols. This heterogeneity is a central finding of this work and a major constraint on cross-source descriptor validation.

---

## 2. Methods

### 2.1 Data

CycPeptMPDB v1.2 was filtered to 7,298 compounds with PAMPA values. For source-stratified analyses, a 1,566-compound subset comprising Furukawa 2016 (individual-compound LC-MS, 1% lecithin/dodecane membrane, DOI: 10.1021/acs.jmedchem.6b01246) and Chugai (patent WO 2013/100132 A1, DOPC/hexadecane membrane) was used as the reference. These are the two most internally consistent sources in the database. Permeability threshold: PAMPA LogPexp ≥ −6.0 log cm/s.

### 2.2 Conformer Generation (Tier-1)

Twenty conformers per molecule were generated using ETKDGv3 with macrocycle torsion sampling enabled, followed by MMFF94s minimization (Wang, Witek, Landrum & Riniker 2020, DOI: 10.1021/acs.jcim.0c00025; Riniker & Landrum 2015, DOI: 10.1021/acs.jcim.5b00654). ETKDGv3 is the RDKit default conformer generator incorporating experimental torsional-angle preferences for macrocycles from crystallographic data. In published benchmarks (CONFORGE 2023, DOI: 10.1021/acs.jcim.3c00563), ETKDGv3 with the macrocycle flag ranks 2nd for macrocycle ring RMSD against crystal structures (0.62 Å), statistically indistinguishable from the best-performing open-source method (0.56 Å). At the scale of this analysis (7,298 compounds), ETKDGv3 is the appropriate choice; CREST-level semiempirical sampling is proposed for validation on a smaller reference set (see Section 5).

The conformer with maximum 3D polar SASA (Bondi radii, RDKit rdFreeSASA) was designated the aqueous conformer and the conformer with minimum 3D polar SASA the membrane conformer. Key descriptors:

- `delta_psa3d` = PSA(aq) − PSA(mem): chameleonic PSA switch
- `psa3d_std`: PSA variance across all 20 conformers (conformational flexibility)
- `delta_hb`, `delta_Rg`, `delta_NPR1/2`: H-bond count, compaction, and shape descriptors

As a negative control, `delta_3DPSA_db` = H2O_3DPSA − CHCl3_3DPSA from CycPeptMPDB's own single-structure 3D PSA values was included to assess whether ensemble sampling is necessary.

### 2.3 Analysis

Features were merged into a 7,298 × 274 matrix. Pearson/Spearman correlations and AUC-ROC were computed for each descriptor. UMAP dimensionality reduction (cosine metric, n_neighbors=30) with dual-track clustering — K-Medoids (k=8, cosine distance) and HDBSCAN (min_cluster_size=50) — was used for chemical space visualization and permeability enrichment analysis. ARI stability was validated across 5 random seeds (threshold ≥ 0.85). Three feature panels were defined: Panel A (2D descriptors), Panel B (3D delta descriptors), Panel C (combined). Track D, introduced in this analysis, adds a fourth subplot coloring the same UMAP embedding by molecular weight.

---

## 3. Results

### 3.1 Reference Compound Validation

| Compound | Permeable | Tier-1 ΔPSA (Å²) | Literature ΔPSA |
|----------|-----------|-----------------|----------------|
| CsA (Cyclosporin A) | Yes | **84.9** | ~75 Å² |
| DP172 | Yes | 88.9 | — |
| HexPep | No | 64.4 | — |
| 1NMe3 | Yes | 47.8 | — |
| PSLYF | No | 65.3 | — |

CsA Tier-1 ΔPSA = 84.9 Å² vs. literature ~75 Å² (within 10%), validating the ensemble approach. Single-structure GFN2-xTB + GBSA optimization gave ΔPSA ≈ 0 Å² for all compounds including CsA, confirming that ensemble sampling is a prerequisite and single-structure solvation is insufficient regardless of level of theory.

### 3.2 AUC-ROC: Descriptor Performance

| Descriptor | 1,566 compounds | Full 7,298 |
|------------|-----------------|------------|
| delta_psa3d (Tier-1 ensemble) | **0.744** | 0.505 |
| MolLogP (2D baseline) | 0.631 | 0.631 |
| delta_3DPSA_db (single-structure) | 0.507 | 0.507 |

Ensemble ΔPSA outperforms MolLogP by 11 AUC points on the source-stratified subset. On the full dataset, ΔPSA collapses to 0.505 (chance). This is attributable to cross-source label noise rather than descriptor failure: Townsend 2020 (~42% of the full dataset, ChemRxiv preprint, DOI: 10.26434/chemrxiv.13335941.v1) uses a pooled 150-compound PAMPA protocol with CycLS MS deconvolution that introduces systematic cross-compound signal interference. The single-structure negative control (delta_3DPSA_db, AUC = 0.507) is stable at both scales, confirming that ensemble sampling — not source stratification alone — drives the 1.5k signal.

### 3.3 UMAP Chemical Space Analysis

**Panel C, 1,566-compound source-stratified subset:**

ARI stability: min = **0.995** across 5 seeds — one dominant structure in combined 2D+3D feature space.

| Cluster | n | Perm rate | Enrichment |
|---------|---|-----------|------------|
| HDBSCAN C0 | 883 | **87.8%** | 1.16× |
| HDBSCAN C1 | 651 | 59.3% | 0.78× |

![Panel C 1566 + Track D](figures/Panel_C_combined_umap_1566.png)

**Track D — Molecular Weight Coloring:**

| Population | Median MW |
|------------|-----------|
| Permeable (PAMPA ≥ −6) | **1,180 Da** |
| Impermeable | 820 Da |
| Ratio | **1.44×** |

The permeable-enriched cluster (C0, 87.8% permeable) occupies the high-MW region of the UMAP (plasma colormap, hot = high MW). Permeable compounds overlaid as limegreen rings concentrate almost entirely in the large-macrolide region of the embedding.

**Panel C, full 7,298-compound dataset:**

![Panel C 6938 + Track D](figures/Panel_C_combined_umap_6938.png)

| Metric | 1,566 compounds | 6,938 compounds |
|--------|-----------------|-----------------|
| HDBSCAN silhouette | 0.425 | **−0.022** |
| Best cluster perm rate | 87.8% | 70.3% |
| Enrichment range | 0.78–1.16× | 0.97–1.03× |
| Median MW permeable | **1,180 Da** | 820 Da |
| Median MW impermeable | 820 Da | 820 Da |

The 1.44× MW gap between permeable and impermeable clusters vanishes on the full dataset — both populations converge to 820 Da. The cross-source PAMPA labels are not merely noisy; they actively invert a biological signal that is present on clean homogeneous data. This is the most direct evidence in this analysis that label quality, not descriptor quality, limits performance at population scale.

---

## 4. Interpretation: Chameleonic Behavior is Size-Gated

The MW gap observed in the 1,566-compound analysis supports a size-gated model of chameleonic permeability. The permeable-enriched cluster is dominated by compounds in the 900–1,500 Da range — consistent with CsA-class macrolides (≥9 residues) that have sufficient backbone flexibility to form stabilizing intramolecular H-bonds in apolar environments and collapse polar surface area.

The impermeable-enriched cluster (C1, 59.3% permeable, median MW 820 Da) is not impermeable per se — it is simply unenriched. Smaller cyclic peptides in the 600–900 Da range may achieve PAMPA permeability through N-methylation (reduced HBD count) or intrinsic lipophilicity without undergoing a ΔPSA switch. For these compounds, absolute ΔPSA is mechanistically inappropriate: insufficient conformational freedom exists to generate a large polar surface collapse.

This interpretation converges with Yu et al. 2026 (*bioRxiv*, DOI: 10.64898/2026.01.06.697862), who independently derive a ≥9-residue size cutoff from the descriptor side: their dimensionless ΔPSA/SASA_total ratio is predictive where absolute ΔPSA fails, and they exclude sub-9-residue compounds on the grounds that chameleonic behavior does not reliably manifest below this size. Two independent lines of evidence — empirical MW enrichment (this work) and normalized descriptor performance (Yu et al.) — converge on the same threshold.

The implication is that absolute ΔPSA (Å²) is a partially confounded descriptor in the current model: it scales with molecular size, so a 15-residue peptide that barely switches produces a larger raw ΔPSA than a 9-residue peptide that completely buries its polar surface. Per-residue normalization (`delta_psa3d / n_residues`) or fractional normalization (`delta_psa3d / SASA_total`) is required to disentangle molecular size from chameleonic efficiency.

---

## 5. Conformer Model and Proposed Validation

ETKDGv3 is appropriate for population-scale screening but is a distance geometry heuristic, not a physics-based simulation of solvent-dependent conformational equilibria. The conformer selection heuristic (max PSA = aqueous, min PSA = membrane) assumes the vacuum ensemble contains conformers representative of solution-phase endpoints — an assumption validated by the CsA result (ΔPSA = 84.9 vs. ~75 Å²) but not systematically tested.

The current state-of-the-art open-source pipeline for macrocyclic peptide conformer generation is the CREMP workflow (Genentech 2024, DOI: 10.1038/s41597-024-03698-y): ETKDGv3 generates an initial diverse pool (up to 5,000 conformers) which CREST then re-ranks using GFN2-xTB energetics. This treats ETKDGv3 and CREST as complementary rather than competing. We propose the following validation on local compute resources:

**Validation set:** 5 reference compounds (CsA, DP172, HexPep, 1NMe3, PSLYF) + 5 randomly selected compounds from each HDBSCAN cluster (C0 and C1) = 15 compounds total.

**Protocol:** ETKDGv3 (5,000 conformers) → CREST re-ranking (GFN2-xTB + ALPB solvation, water then CHCl3) → ΔPSA computed from CREST ensemble.

**Success criterion:** Spearman ρ > 0.8 between ETKDGv3 Tier-1 ΔPSA and CREST ΔPSA across the 15-compound set, and preserved binary permeable/impermeable classification. If the rank order is preserved, ETKDGv3 is validated as sufficient for population-scale pre-filtering. If it is disrupted, ETKDGv3 should be used only as a diversity generator with CREST re-ranking for compounds of interest.

---

## 6. Next Experiments

| Priority | Experiment | Hypothesis |
|----------|------------|------------|
| 1 | Add `delta_psa3d_per_residue`, `delta_psa3d_per_sasa` to `conformer_engine.py`; rerun AUC on 1,566 compounds | Normalized ΔPSA outperforms absolute ΔPSA |
| 2 | Filter `Monomer_Length ≥ 9`; compare AUC with and without cutoff | Chameleonic behavior is residue-gated; cutoff improves signal |
| 3 | ETKDGv3 → CREST validation on 15-compound set | ETKDGv3 rank order is preserved vs. CREST |
| 4 | Exclude Chugai (unverified protocol); rerun on Furukawa only | Chugai inflating or deflating the 1.5k AUC |

---

## References

- Jiang et al. (2023). CycPeptMPDB. *J. Chem. Inf. Model.* DOI: 10.1021/acs.jcim.2c01573
- Riniker & Landrum (2015). ETKDG. *J. Chem. Inf. Model.* DOI: 10.1021/acs.jcim.5b00654
- Wang, Witek, Landrum & Riniker (2020). ETKDGv3. *J. Chem. Inf. Model.* DOI: 10.1021/acs.jcim.0c00025
- Schärfer et al. (2023). CONFORGE benchmark. *J. Chem. Inf. Model.* DOI: 10.1021/acs.jcim.3c00563
- Klarich et al. (2024). CREMP. *Scientific Data*. DOI: 10.1038/s41597-024-03698-y
- Pracht et al. (2020). CREST. *Phys. Chem. Chem. Phys.* DOI: 10.1039/C9CP06869D
- Furukawa et al. (2016). *J. Med. Chem.* DOI: 10.1021/acs.jmedchem.6b01246
- Townsend et al. (2020). *ChemRxiv* (preprint). DOI: 10.26434/chemrxiv.13335941.v1
- Kelly et al. (2021). *J. Am. Chem. Soc.* DOI: 10.1021/jacs.0c06115
- Yu et al. (2026). *bioRxiv*. DOI: 10.64898/2026.01.06.697862
- Witek et al. (2016). *J. Chem. Theory Comput.*
- Bockus et al. (2015). *J. Med. Chem.*
